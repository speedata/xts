---
type: docs
linktitle: CSS Properties
---

<!-- Generated by rake doc from htmlbag.Properties and
     doc/templates/css-properties.txt, do not edit. -->

# CSS Properties Reference

CSS properties supported by XTS for styling layout elements and HTML content. Lengths take the [units](../units) of XTS; `em` and `%` are relative to the font size or the containing block as in CSS.
{{ range .Elements }}
## {{ .Title }}

{{ template "table" . }}{{ end }}
## Selectors

XTS supports the CSS selectors of levels 1 to 3 and the most common of level 4:

| Selector | Example |
|----------|---------|
| Element | `p { ... }` |
| Class | `.highlight { ... }` |
| ID | `#main { ... }` |
| Attribute | `td[align="right"] { ... }` |
| Descendant | `table td { ... }` |
| Child | `table > tr { ... }` |
| Sibling | `h1 + p { ... }`, `h1 ~ p { ... }` |
| Pseudo-class | `tr:nth-child(even) { ... }`, `li:first-child { ... }`, `p:not(.intro) { ... }` |
| Pseudo-element | `h1::before { ... }`, `li::marker { ... }` |
| Multiple | `h1, h2, h3 { ... }` |

## @-rules

### @page

Sets up a master page, see [Master Pages](/manual/page-layout/master-pages#page-setup-from-css). The selector is the name of the master page, `@page` without a name is the default page. A named page inherits the descriptors of the unnamed rule.

{{ template "table" .Page }}
The margin boxes `@top-left-corner`, `@top-left`, `@top-center`, `@top-right`, `@top-right-corner`, `@left-top`, `@left-middle`, `@left-bottom`, `@right-top`, `@right-middle`, `@right-bottom`, `@bottom-left-corner`, `@bottom-left`, `@bottom-center`, `@bottom-right` and `@bottom-right-corner` are nested in the `@page` rule. They take `content`, `width`, `height` and the text and border properties above.

### @font-face

Defines a font face, see [Fonts](/manual/text-and-styling/fonts). The descriptors are:

{{ template "table" .FontFace }}
### @-bag-color

Defines a named color, see [Colors](/manual/advanced/colors#defining-colors-in-css). The color name follows the at-rule, the descriptors are:

{{ template "table" .Color }}
{{ define "table" -}}
| Property | Values | Example |
|----------|--------|---------|
{{ range .Properties }}| {{ .Names }} | {{ .Values }} | `{{ .Example }}` |
{{ end }}{{ end }}
